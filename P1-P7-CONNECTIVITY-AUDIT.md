# 🔍 P1–P7 Connectivity Audit — ผลตรวจระบบและการเชื่อมต่อ
**วันที่:** 22 ส.ค. 2569 | **โดย:** KiloClaw (PM + Developer)
**สถานะ:** รายงานผลตรวจ — รอการอนุมัติแผนเชื่อมต่อ

---

## 📊 สถานะการเชื่อมต่อปัจจุบัน

```
P1 (รับสมัคร) ──✅──▶ P2 (หยั่งประเมิน) ──✅──▶ P3 (จับคู่) ──✅──▶ [hired/รับเข้า] ──▶ บันทึกใน pkg_members
                                                                      │
                                              ┌───────────────────────┘
                                              ▼
                            ❌ P4 ❌ P5 ❌ P6 ❌ P7   (ข้อมูลไม่ไหลมาถึง)
```

| ช่วง | สถานะ | รายละเอียด |
|------|:-----:|------------|
| P1 → P2 | ✅ เชื่อมแล้ว | ผู้สมัครสถานะ `test_done`/`screened` โผล่ใน P2 อัตโนมัติ (อ่านจาก `pkg_applications` โดยตรง — Zero re-entry) |
| P2 → P3 | ✅ เชื่อมแล้ว | ผู้สมัครผ่านประเมิน (`assessed`) ไหลไปหน้าจับคู่ใน P3 |
| P3 → hired | ✅ เชื่อมแล้ว | ปุ่ม "รับเข้าทำงาน" บันทึกตำแหน่ง+วันที่เข้า `pkg_members` |
| hired → P4 | ❌ ขาด | `pkg_members` ไม่ถูกส่งต่อให้โมดูลประเมินผล |
| P4 → P5 | ❌ ขาด | แผนพัฒนาไม่ถูกสร้างอัตโนมัติจากผลประเมิน |
| → P6 / P7 | ❌ ขาด | ค่าตอบแทน/คุณภาพชีวิตแยกข้อมูลของตัวเอง |

---

## 🔬 ผลตรวจเชิงเทคนิค

### 1. ข้อมูลอยู่คนละที่ (3 แหล่ง แยกกัน)
- **`index.html`** (P1–P3 + onboard): ใช้ `pkg_applications`, `pkg_members` ใน localStorage
- **หน้า P4–P7 แยกไฟล์** (`p4-evaluation.html`, `p5-development.html`, `p6-compensation.html`, `p7-wellbeing.html`): ใช้ mock data ในไฟล์ตัวเอง / `PP7_P5_DATA_V1`
- **ไม่มีตัวกลาง:** แต่ละหน้าไม่อ่านข้อมูลของหน้าอื่น

### 2. ข้อจำกัดของสถาปัตยกรรมปัจจุบัน
- **localStorage เป็นของเฉพาะเครื่อง/เบราว์เซอร์** — ข้อมูลที่ทีมเห็นบนเครื่องตัวเอง ไม่เหมือนกันกับของคนอื่น
- **GitHub Pages เป็นเว็บนิ่ง** — ไม่มีเซิร์ฟเวอร์เก็บข้อมูลส่วนกลาง (ที่วางไว้คือ Google Apps Script แต่ยังไม่ต่อ)

### 3. ช่องว่างเทียบกับแผนการไหลข้อมูล (ที่ออกแบบไว้แล้ว)
- กฎ "Evidence-First" ของ P4 — ยังไม่มี
- "Cascade: P4 เสร็จ → สร้างแผนพัฒนา (P5) อัตโนมัติ" — ยังไม่มี
- "P7 แนวโน้มแย่ → แจ้งเตือนหัวหน้า" — ยังไม่มี

---

## 🗺️ แผนเชื่อมต่อที่เสนอ (ไม่แก้รูปแบบหน้าเว็บ)

> หลักการ: **ไม่เปลี่ยนหน้าตา** — เติมเฉพาะ "ท่อน้ำ" ที่เชื่อมข้อมูลระหว่างหน้า

### ระยะที่ 1: ต่อท่อในฝั่งหน้าเว็บ (ทำได้ทันที)
1. สร้างไฟล์ข้อมูลกลาง `member-store.js` — อ่าน/เขียน `pkg_members` จากทุกหน้า
2. หน้า P4–P7 อ่านรายชื่อสมาชิกจากไฟล์กลาง แทนข้อมูลตัวอย่าง
3. ผลประเมินจาก P4 ส่งไปหน้า P5 เพื่อสร้างแผนพัฒนาอัตโนมัติ (ตามกฎ Cascade ที่ออกแบบไว้)

### ระยะที่ 2: ต่อเซิร์ฟเวอร์กลาง (ต้องรอการตัดสินใจ)
4. ตั้ง Google Apps Script เป็นตัวกลางเก็บข้อมูลกลาง (แทนที่ localStorage)
5. หน้าเว็บทุกหน้าเรียกข้อมูลจากแหล่งเดียว — ข้อมูลทีมทุกคนตรงกัน

### ระยะที่ 3: เชื่อมต่อกับระบบจริง
6. เชื่อมกับ Google Sheets "โครงสร้าง ระบบ PP7" (ข้อมูลจริง)
7. แจ้งเตือนผ่าน LINE/Telegram

---

## ⚠️ สิ่งที่รอการตัดสินใจ
| # | เรื่อง | ตัวเลือก |
|---|--------|----------|
| 1 | เริ่มทำระยะที่ 1 เลยไหม | ✅ ทำทันที / ⏸️ รอ |
| 2 | ใช้ Google Apps Script เป็นตัวกลางได้ไหม | ต้องขอสิทธิ์จากพี่ปกรณ์ |
| 3 | ข้อมูลจริงพร้อมหรือยัง | รอทีมเตรียม |

---
*เอกสารนี้สร้างโดย KiloClaw 2026-08-22 — ส่วนหนึ่งของงานเชิงรุกในบทบาท PM + Developer*
